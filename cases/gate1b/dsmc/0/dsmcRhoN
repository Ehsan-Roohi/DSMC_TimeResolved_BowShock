FoamFile
{
    format ascii;
    class volScalarField;
    location "0";
    object dsmcRhoN;
}
dimensions [0 -3 0 0 0 0 0];
internalField uniform 0;
boundaryField
{
    xMax { type cyclic; }
    xMin { type cyclic; }
    yMax { type cyclic; }
    yMin { type cyclic; }
    zMax { type cyclic; }
    zMin { type cyclic; }
}
