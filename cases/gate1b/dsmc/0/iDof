FoamFile
{
    format ascii;
    class volScalarField;
    location "0";
    object iDof;
}
dimensions [0 -3 0 0 0 0 0];
internalField uniform 1e-300;
boundaryField
{
    xMax { type cyclic; }
    xMin { type cyclic; }
    yMax { type cyclic; }
    yMin { type cyclic; }
    zMax { type cyclic; }
    zMin { type cyclic; }
}
