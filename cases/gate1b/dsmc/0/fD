FoamFile
{
    format ascii;
    class volVectorField;
    location "0";
    object fD;
}
dimensions [1 -1 -2 0 0 0 0];
internalField uniform (0 0 0);
boundaryField
{
    xMax { type cyclic; }
    xMin { type cyclic; }
    yMax { type cyclic; }
    yMin { type cyclic; }
    zMax { type cyclic; }
    zMin { type cyclic; }
}
