VALIDATION REPORT
=================

1. Python syntax check: PASS.
2. Internal synthetic translation test: PASS.
   - known displacement amplitude was recovered with correlation > 0.95;
   - median spatial template correlation exceeded 0.75.
3. The production script imports the already validated DS2FF reader,
   adaptive-grid remapper, half-jump registration and physical-domain
   attached-window sampler from the unified analysis package.
4. Production raw-data execution was not possible in this environment because
   the Windows DS2FF paths are not mounted.
5. The code processes one case at a time and writes a compressed attached-field
   cache, so interrupted/repeated runs do not need to rebuild completed cases.
